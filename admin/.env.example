# API base URL (must match the API server). Default: http://localhost:8000
# VITE_API_URL=http://localhost:8000
#
# To create an admin user, run in the api folder: pnpm run db:seed
# Default credentials: admin@example.com / admin123 (or set ADMIN_EMAIL, ADMIN_PASSWORD in api .env)
